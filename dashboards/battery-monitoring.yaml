# Battery Monitoring Dashboard - Built-in Cards Only
title: Battery Monitoring
path: battery-monitoring
badges: []
cards:
  # Simple built-in card test
  - type: markdown
    content: |
      # Battery Dashboard Test
      If you see this, the dashboard is loading correctly.
  
  # Test with a known entity using built-in entity card
  - type: entity
    entity: sun.sun
    name: "Test Entity"
  
  # Show current time to verify dashboard is working
  - type: glance
    title: "Dashboard Test"
    entities:
      - entity: sun.sun
        name: "Sun"
